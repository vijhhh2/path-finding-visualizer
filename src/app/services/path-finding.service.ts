import { Injectable } from '@angular/core';
import { CellNode } from '../components/models/node.model';

@Injectable({
  providedIn: 'root'
})
export class PathFindingService {

  constructor() { }

  bfsPathFinding(start: CellNode, end: CellNode) {
    
  }
}
